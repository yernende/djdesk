{
  "name": "@djdesk/client",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm run build --workspace @djdesk/domain && vue-tsc -p tsconfig.app.json --noEmit && vite build",
    "dev": "vite --host 0.0.0.0",
    "preview": "vite preview",
    "typecheck": "npm run build --workspace @djdesk/domain && vue-tsc -p tsconfig.app.json --noEmit && vue-tsc -p tsconfig.node.json --noEmit"
  },
  "dependencies": {
    "@djdesk/domain": "0.1.0",
    "vue": "^3.5.32"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^6.0.6",
    "vite": "^8.0.8",
    "vue-tsc": "^3.2.7"
  }
}
